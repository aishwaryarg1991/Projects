package com.sundogsoftware.spark

import org.apache.spark._
import org.apache.spark.SparkContext._
import org.apache.log4j._

object LearningScala {
  
    def main(args: Array[String]) {
  val hello: String = "Hola!"
  
  
  // EXERCISE
  // Write some code that takes the value of pi, doubles it, and then prints it within a string with
  // three decimal places of precision to the right.
  // Just write your code below here; any time you save the file it will automatically display the results!
  
  val pi1 : Float = 3.142142f  //
 println(f"The pi value doubled Like ${2* pi1}%.3f") //
    }
    
 
    def fib1( n : Int) : Int =
   n match {
   case 0 | 1 => n
   case _ => fib1( n-1 ) + fib1( n-2 )
}
 
    def fib2( n : Int ) : Unit = {
  var a = 1
  var b = 1
  var i = 0	  
 
  while( i < n ) {
    val c = a + b
     //print(s"$a"x)
    a = b
    b = c
    i = i + 1
   // print(a)
  } 
  //println( a)
}
 fib2(10)
 
 
 def foo(str1:String) : String = {
  
  str1.toUpperCase();
  
}

println(foo("ais"))
 
}



